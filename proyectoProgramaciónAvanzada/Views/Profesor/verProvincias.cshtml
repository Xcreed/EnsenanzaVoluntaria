@model IEnumerable<proyectoProgramaciónAvanzada.Models.PROVINCIA>

@{
    ViewBag.Title = "verProvincias";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>verProvincias</h2>

<p>
    @Html.ActionLink("Create New", "crearProvincia")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DES_PROVINCIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAIS.DES_PAIS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DES_PROVINCIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAIS.DES_PAIS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PROVINCIA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PROVINCIA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PROVINCIA })
        </td>
    </tr>
}

</table>
